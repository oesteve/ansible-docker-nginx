---
# vars file for ansible-docker-nginx

nginx_conf_dir: "{{ services_dir }}/nginx/conf"

lets_encrypt_web_root: "{{ services_dir }}/letsencrypt/webroot"
lets_encrypt_dir: "{{ services_dir }}/letsencrypt"
